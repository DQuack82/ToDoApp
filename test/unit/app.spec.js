import { expect } from 'chai';
import sinon from 'sinon';

import fileToTest from '../../src/app'

describe('Check All Items', () => {
  it('should check all items', () => {
    expect(true).to.equal(true);
  });
});